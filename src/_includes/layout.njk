<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="preconnect" href="https://gc.zgo.at" crossorigin />
    <link rel="dns-prefetch" href="https://gc.zgo.at" />

    <title>{{ title }} - sickfunny</title>
    <meta name="description" content="{{ description }}">
    {% if author %}<meta name="author" content="{{ author }}">{% endif %}
    <link rel="canonical" href="https://sickfunny.co{{ page.url }}">

    {# Open Graph #}
    <meta property="og:type" content="article">
    <meta property="og:title" content="{{ title }} - sickfunny">
    <meta property="og:description" content="{{ description }}">
    <meta property="og:url" content="https://sickfunny.co{{ page.url }}">
    <link rel="icon" href="/favicon.png" type="image/png">
    <link rel="alternate" type="application/atom+xml" href="/feed.xml" title="sickfunny">
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#ffffff" media="(prefers-color-scheme: light)">
    <meta name="theme-color" content="#000000" media="(prefers-color-scheme: dark)">
    <style>{% include "../css/styles.css" %}</style>
  </head>
  <body>

    <header>
      <a href="/" rel="home" class="title"><strong>sick</strong>funny &#215;&#41;</a>
      <a href="/feed.xml">rss.</a>
    </header>

    <main>
      {{ content | safe }}
    </main>
    
    <footer>
      log: build completed — 0 warnings, 0 errors<br>
      log: loaded in <span id="load-time"></span>
    </footer>

    <script>
      onload = () => {
        const [n] = performance.getEntriesByType('navigation');
        document.getElementById('load-time').textContent = `${Math.round(n?.domContentLoadedEventEnd)}ms`;
      };
    </script>
    <script data-goatcounter="https://sickfunny.goatcounter.com/count" async src="//gc.zgo.at/count.js"></script>
  </body>
</html>
